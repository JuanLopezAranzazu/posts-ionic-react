import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'prueba-posts-ionic',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
